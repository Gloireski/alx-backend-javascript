/**
 * Displays a message on the STDOUT.
 * @param {String} par The message to display.
 * @author Belem Gloire BEKOUTOU <https://github.com/Gloireski>
 */
const displayMessage = (par) => {
    console.log(par);
}

module.exports = displayMessage;
